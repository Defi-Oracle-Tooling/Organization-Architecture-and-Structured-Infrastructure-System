{
  "name": "@absolute-realms/monorepo-template",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*",
    "contracts/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md,sol}\""
  },
  "devDependencies": {
    "turbo": "^1.10.0",
    "prettier": "^2.8.8",
    "typescript": "^5.0.0"
  }
}
